- eliminate critical edge
- watch out when dom bb is nullptr
- bug: generating new temporary during phi_translate
- 单条expr的phi_trans需要完善一下，通过val进行翻译！

```c
int V[200][200]={};
int KnapSack(int n, int w[], int v[], int x[], int C)
{
    int i, j;
    i=1;
    while(i<=n)
    {
    	j=0;
    	while(j<C+1)
    	{
            V[i][j] = V[i - 1][j];
    		j=j+1;
		}
		i=i+1;
	}
    return 0;
}

int main()
{
   
    return 0;

}

```
上面的代码会引起求支配树错误！入口块仍然具有idom